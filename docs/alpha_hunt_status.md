# Alpha Hunt Status

This file is generated by:

```bash
uv run python -m ofbot.cli alpha-loop run --config config/local.paper.yaml --once
```

The runtime loop rewrites this page with the latest offline candidate, runtime canary, and active paper config.
